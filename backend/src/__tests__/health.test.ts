import { describe, expect, it } from "vitest";

// Placeholder so the test runner and folder structure are wired up from
// commit one. Real service/repository tests land alongside the code they
// cover as each feature is built.
describe("project setup", () => {
  it("runs tests", () => {
    expect(true).toBe(true);
  });
});
