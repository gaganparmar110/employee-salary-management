// Deterministic seed for 10,000 employees (see docs/REQUIREMENTS.pdf, item 7).
// Intentionally left as a stub in this initialization commit — the seed logic
// is built in its own step, once EmployeeService exists and this script has
// a real write path to call into.

async function main() {
  console.log("Seed script placeholder — implementation tracked separately.");
}

main();
